<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.android.tourguideapp.PlaceDetailsActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/detailed_text_padding">

        <ImageView
            android:id="@+id/detailed_image"
            style="@style/ImageStyle"
            android:layout_width="@dimen/detailed_image_width"
            android:layout_height="@dimen/detailed_image_height"
            android:layout_gravity="center_horizontal"
            tools:src="@drawable/legoland"
            android:contentDescription="@string/legoland_name"/>

        <TextView
            android:id="@+id/detailed_name"
            style="@style/BigTextStyle"
            android:layout_gravity="center"
            android:layout_marginTop="@dimen/list_marginLeft"
            android:textAlignment="center"
            tools:text="@string/legoland_name" />

        <TextView
            android:id="@+id/detailed_address"
            style="@style/DetailedTextStyle"
            android:layout_marginTop="0dp"
            tools:text="@string/legoland_address" />

        <TextView
            android:id="@+id/detailed_desc"
            style="@style/DetailedTextStyle"
            tools:text="@string/legoland_description" />

        </LinearLayout>

</ScrollView>
